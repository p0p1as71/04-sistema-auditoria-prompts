description: "Validación M12 - Scoring hasta 9"

prompts:
  - file://prompts_maestros.md

providers:
  - id: openai:gpt-4o-mini
    config:
      temperature: 0

tests:
  - vars:
      modulo: "M12_Scoring"
    assert:
      - type: javascript
        value: output.includes('scoring') && output.includes('precision')